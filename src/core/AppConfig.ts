export const AppConfig = {
    Settings: {
        Server: {
            baseUrl: "https://sidebet-api.herokuapp.com",
            
            // baseUrl: "http://localhost:3000",
            timeout: 30000,
            context: "SideBet",
        }
    }
};